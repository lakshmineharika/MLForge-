# fs2db - FileStore to Database Migration Tool

Migrate MLForge FileStore (`./mlruns`) data to a database backend.

```bash
MLForge migrate-filestore --source ./mlruns --target sqlite:///MLForge.db
```
