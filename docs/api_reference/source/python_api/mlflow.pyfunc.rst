MLForge.pyfunc
=============

.. automodule:: MLForge.pyfunc
    :members:
    :undoc-members:
    :show-inheritance:

.. Include ``get_default_pip_requirements`` and ``get_default_conda_env``,
   which are imported from `MLForge.pyfunc.model`, in the `MLForge.pyfunc` namespace
.. autofunction:: MLForge.pyfunc.get_default_pip_requirements
.. autofunction:: MLForge.pyfunc.get_default_conda_env

.. Include ``PythonModelContext`` as a renamed class to avoid documenting constructor parameters.
   This class is meant to be constructed implicitly, and users should only be aware of its
   documented member properties.
.. autoclass:: MLForge.pyfunc.PythonModelContext()
    :members:
    :undoc-members:

.. Include ``PythonModel``, which is imported from `MLForge.pyfunc.model`, in the
   `MLForge.pyfunc` namespace
.. autoclass:: MLForge.pyfunc.PythonModel
    :members:
    :undoc-members:

.. Include ``ChatModel``, which is imported from `MLForge.pyfunc.model`, in the
   `MLForge.pyfunc` namespace
.. autoclass:: MLForge.pyfunc.ChatModel
    :members:
    :undoc-members:

.. Include ``ChatAgent``, which is imported from `MLForge.pyfunc.model`, in the
   `MLForge.pyfunc` namespace
.. autoclass:: MLForge.pyfunc.ChatAgent
    :members:
    :undoc-members:

.. Include ``ResponsesAgent``, which is imported from `MLForge.pyfunc.model`, in the
   `MLForge.pyfunc` namespace
.. autoclass:: MLForge.pyfunc.ResponsesAgent
    :members:
    :undoc-members:
